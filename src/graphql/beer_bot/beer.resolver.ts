// Boilerplate GraphQL resolver for BeerBot
// No runtime functionality included — placeholders only

import { Resolver } from '@nestjs/graphql';

@Resolver()
export class BeerResolver {
    // Resolver methods will be added here.
}

export default BeerResolver;
